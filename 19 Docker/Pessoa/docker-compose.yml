version: '3.1'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    networks:
      rede_um:
         master
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: 1996